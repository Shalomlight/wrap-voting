[project]
name = "wrap-voting"
description = "Decentralized governance system with token-based voting"
authors = ["Wrap Voting Team"]
telemetry = false
requirements = []
boot_loading = false

[contracts.governance-token]
path = "contracts/governance-token.clar"
clarity_version = 2
deployment_fee_cap = 15000

[repl.analysis]
passes = ["check_functions"]